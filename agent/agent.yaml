spec_version: "0.1.0"
name: regret-minimizer
version: 1.0.0
description: Mines git history and past decisions to help you avoid repeating mistakes

model:
  preferred: "openai:gpt-4o"
  fallback: ["openai:gpt-4o-mini"]
  constraints:
    temperature: 0.2
    max_tokens: 512

tools: [read, write, memory]

runtime:
  max_turns: 5
  timeout: 60
